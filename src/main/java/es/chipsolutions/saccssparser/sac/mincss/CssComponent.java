package es.chipsolutions.saccssparser.sac.mincss;

public class CssComponent {
/*
	public static CssComponent newOldFormatInstance(String tipoFoto,int photoCount) {
		BufferedImage img = new BufferedImage(40, 40, BufferedImage.TYPE_INT_RGB);
	    Graphics2D g = img.createGraphics();
	    g.setColor(Color.WHITE);
	    g.fillOval(x, y, width, height);
		
		
		
		
		 Bitmap bitMap = Bitmap.createBitmap(100, 100, Bitmap.Config.ARGB_8888);  //creates bmp
		    bitMap = bitMap.copy(bitMap.getConfig(), true);     //lets bmp to be mutable
		    Canvas canvas = new Canvas(bitMap);                 //draw a canvas in defined bmp

		    Paint paint = new Paint();                          //define paint and paint color
		    paint.setColor(Color.RED);
		    paint.setStyle(Style.FILL_AND_STROKE);
		    //paint.setAntiAlias(true);

		    canvas.drawCircle(50, 50, 10, paint);
	}

*/	
}
